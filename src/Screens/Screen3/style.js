import {StyleSheet} from 'react-native';
export const styles = () =>
  StyleSheet.create({
    image: {
      width: '40%',
      height: undefined,
      aspectRatio: 1,
      resizeMode: 'contain',
    }
  });
